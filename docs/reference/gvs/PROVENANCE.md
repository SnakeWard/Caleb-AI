# GVS reference artifacts — provenance

**Authored by Pat** as a standalone Gemini Voice Studio (Multi-Engine Edition; correction protocol GVS-ME-CORR-01). **Standalone origin** outside Caleb AI. **Date received into this repository:** 2026-08-04 (GVS-0). **Not integrated** into Caleb runtime, catalogs, providers, CLI, or tests. **Not executable within Caleb** — the HTML is a browser-side reference application only; the PDF is a user manual. **Credential handling is below house standard** (empty `apiKey` literal with `?key=${apiKey}` URL shaping in the HTML; no live key is present in the committed bytes, but the pattern must not be modeled). **Do not model from it** for production adapters, credential closures, or trust promotion. The two binary/text artifacts below are stored **unmodified** (byte-identical to the sources provided for GVS-0); this provenance file is a companion header and is not part of either artifact.

| File | Role |
| --- | --- |
| `gemini_voice_studio.html` | Single-file Multi-Engine GVS application (reference only) |
| `Gemini Voice Studio - User Manual.pdf` | GVS user manual (reference only) |
