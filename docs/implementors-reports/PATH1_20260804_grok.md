# PATH-1 — Canonical Repository Path

**Pass ID:** PATH-1  
**Seat:** Implementer — Grok 4.3 (Grok Build TUI)  
**Authorized by:** Pat (PATH-1 protocol + approval to keep canonical home on D despite exFAT)  
**Date:** 2026-08-04  
**Protocol:** `docs/protocols/PASS_PROTOCOL_PATH1.md`  
**HEAD at start:** `4d926ca` (REC-1)  
**Working path:** `D:\Caleb AI`  
**Prechange:** `snap_20260804T054901409Z_000478_milestone` (`path1_canonical_repository_path_prechange`) — verified on disk before recording

A recap is not a report.

---

## Summary

PATH-1 records **`D:\Caleb AI` (`exFAT`)** as the single canonical working copy, amends preflight so directory identity is checked **before** tree cleanliness, annotates the DEBT-1 vs reality contradiction without rewriting accepted DEBT-1 text, adopts bare-mirror backup (forbids folder-sync between working clones), and retires the stale F working clone. Docs/process only (plus out-of-repo archival). SEG-C not started.

## Drive / filesystem evidence (§4A)

| Drive | Label | Filesystem | Size (approx) | Free (approx) |
| --- | --- | --- | --- | --- |
| D: | Extreme SSD | **exFAT** | ~931 GiB | ~18 GiB |
| F: | Local Disk 2 | **NTFS** | ~1.86 TiB | ~165 GiB |

| Clone | HEAD (pre-pass) | Tree |
| --- | --- | --- |
| `D:\Caleb AI` | `4d926ca` REC-1 = `origin/main` | clean |
| `F:\Caleb AI` | `7002513` SEAL-D1 (four commits behind) | dirty: `M .caleb/ledger/ledger.jsonl` |

**Canonical path and filesystem as recorded:** `D:\Caleb AI` (`exFAT`, PATH-1, 2026-08-04).  
**Pat disposition:** keep canonical home despite non-NTFS (live history wins over filesystem preference).

## DEBT-1 reconciliation outcome

| Question | Answer |
| --- | --- |
| Contradiction found? | **yes** |
| DEBT-1 said | `F:\Caleb AI` authoritative home; D cold backup |
| Reality at PATH-1 | D holds live history; F stale second clone |
| Resolution | Canonical = `D:\Caleb AI` (exFAT); DEBT-1 body **not** rewritten; addendum on `docs/DEBT_1_MIGRATION_RECONCILIATION_REPORT.md` + STATUS_LOG PATH-1 entry |

## F: pre-archival HEAD and ledger-append disposition

| Field | Value |
| --- | --- |
| Pre-archival HEAD | `7002513758b2b3f4215a5ebadd3e6d69fad4d544` — SEAL-D1 |
| Dirty list | `M .caleb/ledger/ledger.jsonl` only (+1 line) |
| Dirty `ledger_id` / `snapshot_id` | `ledger_snap_20260722T184249824Z_000023_milestone` / `snap_20260722T184249824Z_000023_milestone` |
| Present in D ledger? | **No** (unique to F working tree) |
| Local snapshot dir on F? | **Yes** (gitignored local change-guard state) |
| **Disposition** | **Preserve-by-archive-rename** — leave dirty line and local snapshot inside the archived tree; do not commit into F; do not merge into canonical. Stale local snapshot activity of the wrong clone; not needed on D. |

### §4E rename status

| Attempt | Result |
| --- | --- |
| Target | `F:\Caleb AI` → `F:\Caleb AI.ARCHIVED-20260804` |
| Outcome this session | **Blocked:** directory in use by another process (implementer host workspace bound to `F:\Caleb AI`; process name `grok`) |
| Operator finish (after closing any session rooted on F:) | `Rename-Item -LiteralPath 'F:\Caleb AI' -NewName 'Caleb AI.ARCHIVED-20260804'` |

No commit was made into F. Canonical work is entirely on D. Until the operator rename lands, F remains a **non-canonical stale tree that must not be used for passes**.

## Mirror decision

**Adopted.** One-line argument: a bare mirror has no working tree and therefore cannot become a second source of truth or host an accidental pass; folder-sync between working clones is forbidden because two writers to an append-only ledger diverge.

Refresh (when operator creates the mirror):

```
git clone --mirror https://github.com/SnakeWard/Caleb-AI.git F:\caleb-ai-mirror.git
git --git-dir=F:\caleb-ai-mirror.git remote update --prune
```

## Preflight amendment text as committed

Standing order: (1) confirm cwd + `git rev-parse --show-toplevel` equal `D:\Caleb AI`; (2) `git status --short`; (3) `git log --oneline -3` and remote-sync. PowerShell guard and **Working path** mandatory report line committed in `docs/01_CODEX_OPERATING_CONTRACT.md` sections *Canonical Repository Path*, *Preflight Directory Assertion*, amended *Handoff Discipline*, and *Completion Report Format*.

## Files

**Created:**

- `docs/protocols/PASS_PROTOCOL_PATH1.md`
- `docs/implementors-reports/PATH1_20260804_grok.md`
- `examples/audit/path1-pass-manifest.valid.json`

**Changed:**

- `docs/01_CODEX_OPERATING_CONTRACT.md`
- `docs/DEBT_1_MIGRATION_RECONCILIATION_REPORT.md` (addendum only)
- `docs/STATUS_LOG.md`
- `PLANS.md`
- `.caleb/ledger/ledger.jsonl` (append-only prechange snapshot)

**Intentionally not changed:** `src/**`, `tests/**`, catalogs, package/config, egress, SEG-C.

## Validation

| Command | Result |
| --- | --- |
| Working path | `D:\Caleb AI` |
| `node ./node_modules/typescript/bin/tsc --noEmit` | exit **0** |
| `npx vitest run` | **213** files / **3294** tests passed, exit **0** |
| Catalogs | **14 / 9** |
| AUD-2 self-smoke | **compliant / T2**, 0 violations (manifest `examples/audit/path1-pass-manifest.valid.json`, base-ref `4d926ca`) |
| Tree / remote | clean after push (recorded at commit close) |

**Suite counts verbatim:** Test Files 213 passed (213); Tests 3294 passed (3294).  
**Catalogs:** **14/9**.

## Honest deviations

1. **§4E rename incomplete in-session:** F directory locked by the implementer host workspace rooted on `F:\Caleb AI`. Disposition of the unique ledger append is decided (preserve in archive); physical rename deferred to operator command after F-bound sessions close. Not a second working clone authorization.
2. **exFAT exception:** protocol preferred NTFS; Pat explicitly authorized D despite exFAT after STOP report.

## Verdict

`PATH-1 Canonical Repository Path: Accepted — one truth, one dumb backup, and a preflight that catches the wrong tree before the wrong byte.`

*(Acceptance note: operator rename of F remains required to fully retire the second working tree; contract and preflight already forbid using it.)*
