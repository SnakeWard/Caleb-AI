# REC-1 — Crown Event Reports

**Pass ID:** REC-1  
**Seat:** Implementer — Grok 4.3 (Grok Build TUI)  
**Date:** 2026-08-04  
**Protocol:** `docs/protocols/PASS_PROTOCOL_REC1.md`  
**HEAD at start:** `6f7b56f` (SYNC-1)  
**Prechange:** `snap_20260804T023242878Z_000476_milestone` (`rec1_crown_event_reports_prechange`) — verified on disk

A recap is not a report.

---

## Summary

REC-1 assembles (does not invent) crown acceptance reports for LIVE-R2-E1-A8 and LIVE-R2-E2-A1 from ledger + register + offline reconstruction; prepends a supersession notice to the early E1 event report without altering its body; and names the **authorization-after-the-fact** defect class in the operating contract with its three citations and the register-before-evidence test.

## A8 reconstruction result (verbatim mandatory)

Offline `reconstructRotationChainFromLedgerJsonl` against plan `plan_a108ac09-63fc-57aa-81b5-a44ad276aa02` / execution `execution_11850824-74ad-46f9-b7ff-3dc67cbd54b8`:

| Field | Value |
| --- | --- |
| `ok` | **true** |
| `refusal_code` | null |
| `errors` | `[]` |
| `chain.final_status` | **`completed`** |
| `chain.completed_steps` | **2** |
| `chain.failed_step` | null |
| Role order | planner → critic |
| Totals (ledger) | 4110 tokens / $0.012526 / 2 inv under 8192 / $0.05 / 2 |

**Anomaly:** none. Full assembly: `docs/LIVE_R2_E1_A8_ACCEPTANCE_REPORT.md`.

E2 reconstruction (same method): `ok: true`, `final_status: completed`, `completed_steps: 2`, 3181 tokens / $0.00615 — `docs/LIVE_R2_E2_A1_ACCEPTANCE_REPORT.md`.

## NOT IN RECORD count (mandatory)

| Report | Count | Items (summary) |
| --- | --- | --- |
| A8 acceptance | **4** | event sibling ABSENT; event leaf ABSENT; dedicated findings list; pre-run register |
| E2-A1 acceptance | **8** | two-key sibling ABSENT ×2; two-key leaf ABSENT ×2; host-shell transcript; pre-run register |
| **Total** | **12** | |

## Supersession body unchanged (affirmative)

`docs/LIVE_R2_E1_EVENT_REPORT.md`: supersession header **prepended only**.  
Pre-pass body length 11695 chars (normalized LF); post-pass body starting at `# LIVE-R2 Event E1 Report` matches pre-pass body byte-for-byte on normalized text comparison (`BODY_UNCHANGED=True`).

## Defect-class text as committed

In `docs/01_CODEX_OPERATING_CONTRACT.md`, immediately after the live-event authorization record section:

> **Authorization-after-the-fact (defect class).** Live-event authorization recorded retroactively rather than before execution. Citations: LIVE-R2-E1-A7/A8 (words registered post-run, found at AUTH-2); LIVE-R2-E2-A1 (executed with no pre-run register entry, appended at AUTH-3); LIVE-D1-A1 (register entry missing at evidence time, sealed at SEAL-D1). Structural fixes in force: AUTH-2 (register entry required before execution) and AUTH-3 (implementer echoes the pending authorization requirement at event start; operator does not proceed until the entry is appended). The test of the fix is simple and must be applied to every future live event: **the register entry's commit must precede the evidence commit.** Any future event where it does not is a fourth citation and a reportable finding.

## Files

**Created:** A8 report; E2-A1 report; this implementer report; `docs/protocols/PASS_PROTOCOL_REC1.md`; `examples/audit/rec1-pass-manifest.valid.json`  

**Changed:** E1 event report (header only); operating contract (defect class); STATUS_LOG; PLANS; ledger append (snapshot)

**Intentionally not changed:** `src/**`, `tests/**`, catalogs, credentials, live execution, existing report bodies (except E1 header prepend)

## Validation

| Command | Result |
| --- | --- |
| `node ./node_modules/typescript/bin/tsc --noEmit` | exit **0** |
| `npx vitest run` | **213** files / **3294** tests, exit **0** |
| Catalogs | **14 / 9** |
| AUD-2 | **compliant / T2**, 0 violations (base-ref `6f7b56f`) |
| Tree / remote | clean after push |

## Honest deviations

1. **E2 report path:** new file `docs/LIVE_R2_E2_A1_ACCEPTANCE_REPORT.md` (no prior E2 acceptance report exists to append to).  
2. Temporary reconstruction helper scripts under `.tmp-rec1-*` — not committed (workspace hygiene).

## Verdict

`REC-1 Crown Event Reports: Accepted — the campaign's completed events are documented to the standard of its failures; the authorization defect class is named with its citations.`
