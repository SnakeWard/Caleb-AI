# Caleb AI Agent Guidance

Caleb AI is a Hollow-first orchestration system.

Doctrine: **Models think. Hollows work. Caleb orchestrates.**

Codex MUST read `docs/00_SOURCE_INDEX_AND_AUTHORITY.md` first, then read `docs/01_CODEX_OPERATING_CONTRACT.md` before any implementation. Codex MUST obey `docs/02_V1_PHASE_BOUNDARIES.md` and MUST NOT implement beyond the currently authorized pass.

Use `PLANS.md` for multi-step or multi-file tasks.

V1 does not include role rotation, model provider APIs, complex UI, 3D Thinking Mode, cloud deployment, or production auth.

Once Auto Snapshot and Change Guard exists, Codex MUST snapshot before mutation. Before it exists, Codex MUST use git or manual snapshot discipline.

Codex MUST NOT leave placeholders, stubs, fake tests, or TODOs presented as complete. Codex MUST run tests, typecheck, and build when available before claiming done.

At the end of this pass, Codex MUST report files created or changed and stop.
